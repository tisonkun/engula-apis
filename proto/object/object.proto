syntax = "proto3";

package engula.v1;

import "object/numeric.proto";

message TypeSpec {
  oneof spec { Int64Spec int64_spec = 1; }
}

message Int64Spec {}

message TypedExpr { repeated NumericExpr numeric = 1; }

message TypedValue { repeated NumericValue numeric = 1; }
